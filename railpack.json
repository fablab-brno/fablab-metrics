{
	"provider": "node",
	"packages": {
		"node": "24",
		"python": "3.13"
	},
	"steps": {
		"install": {
			"commands": ["...", "copy ./pyproject.toml ./pyproject.toml", "copy ./poetry.lock ./poetry.lock", "ls", "echo app content:", "ls /app", "poetry install"]
		},
		"build": {
			"inputs": [{ "step": "install" }],
			"commands": ["..."]
		}
	}
}